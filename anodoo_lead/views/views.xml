<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="anodoo_lead.list">
      <field name="name">anodoo_lead list</field>
      <field name="model">anodoo_lead.anodoo_lead</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="anodoo_lead.action_window">
      <field name="name">anodoo_lead window</field>
      <field name="res_model">anodoo_lead.anodoo_lead</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="anodoo_lead.action_server">
      <field name="name">anodoo_lead server</field>
      <field name="model_id" ref="model_anodoo_lead_anodoo_lead"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="anodoo_lead" id="anodoo_lead.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="anodoo_lead.menu_1" parent="anodoo_lead.menu_root"/>
    <menuitem name="Menu 2" id="anodoo_lead.menu_2" parent="anodoo_lead.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="anodoo_lead.menu_1_list" parent="anodoo_lead.menu_1"
              action="anodoo_lead.action_window"/>
    <menuitem name="Server to list" id="anodoo_lead" parent="anodoo_lead.menu_2"
              action="anodoo_lead.action_server"/>
    -->
  </data>
</odoo>